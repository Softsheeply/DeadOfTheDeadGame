# xolo — incoming walk sheets

Drop ChatGPT PNGs here, then slice:

```bash
python3 scripts/slice_character_incoming.py --character xolo
```

Files: `xolo_walk_down.png`, `xolo_walk_up.png`, `xolo_walk_left.png`, `xolo_idle_down.png`

See `docs/art-briefs/cast-walk-pipeline.md`.
