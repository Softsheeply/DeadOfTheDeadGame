# miguel — incoming walk sheets

Drop ChatGPT PNGs here, then slice:

```bash
python3 scripts/slice_character_incoming.py --character miguel
```

Files: `miguel_walk_down.png`, `miguel_walk_up.png`, `miguel_walk_left.png`, `miguel_idle_down.png`

See `docs/art-briefs/cast-walk-pipeline.md`.
