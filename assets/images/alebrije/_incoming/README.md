# alebrije — incoming walk sheets

Drop ChatGPT PNGs here, then slice:

```bash
python3 scripts/slice_character_incoming.py --character alebrije
```

Files: `alebrije_walk_down.png`, `alebrije_walk_up.png`, `alebrije_walk_left.png`, `alebrije_idle_down.png`

See `docs/art-briefs/cast-walk-pipeline.md`.
